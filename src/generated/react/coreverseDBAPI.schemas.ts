/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Coreverse DB API
 * Centralized data-access API for the Coreverse ecosystem. Coreverse DB defines and serves all data operations; Coreverse Launcher and Coreverse Website consume this API and never access Postgres/Supabase directly.
 *
 * OpenAPI spec version: 0.2.0
 */
/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export interface Ok {
  ok: boolean;
}

export type ReleaseStatus = (typeof ReleaseStatus)[keyof typeof ReleaseStatus];

export const ReleaseStatus = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type ReleaseArtifactsItemOs =
  (typeof ReleaseArtifactsItemOs)[keyof typeof ReleaseArtifactsItemOs];

export const ReleaseArtifactsItemOs = {
  windows: "windows",
  linux: "linux",
  macos: "macos",
} as const;

export type ReleaseArtifactsItemArchitecture =
  (typeof ReleaseArtifactsItemArchitecture)[keyof typeof ReleaseArtifactsItemArchitecture];

export const ReleaseArtifactsItemArchitecture = {
  x86_64: "x86_64",
  arm64: "arm64",
} as const;

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ReleaseArtifactsItemMinRequirements = { [key: string]: unknown };

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ReleaseArtifactsItemCompiler = { [key: string]: unknown };

/**
 * A downloadable, per-OS/architecture build of a release. sha256 is used by the Launcher to verify binary integrity in case the distribution source is ever compromised.
 */
export type ReleaseArtifactsItem = {
  os: ReleaseArtifactsItemOs;
  architecture: ReleaseArtifactsItemArchitecture;
  download_url: string;
  /**
   * Lowercase hex-encoded SHA-256 of the artifact file.
   * @pattern ^[a-f0-9]{64}$
   */
  sha256: string;
  size_bytes: number;
  /** Free-form (validated at the API layer, not by this schema alone). */
  min_requirements: ReleaseArtifactsItemMinRequirements;
  /** Free-form (validated at the API layer, not by this schema alone). */
  compiler: ReleaseArtifactsItemCompiler;
};

/**
 * Coreverse Engine release metadata. Contains no binaries -- see artifacts[] for per-OS/architecture downloads.
 */
export interface Release {
  /** @pattern ^\d+\.\d+\.\d+$ */
  version: string;
  status: ReleaseStatus;
  release_date: string;
  release_notes_summary?: string | null;
  artifacts: ReleaseArtifactsItem[];
}

export type ArtifactOs = (typeof ArtifactOs)[keyof typeof ArtifactOs];

export const ArtifactOs = {
  windows: "windows",
  linux: "linux",
  macos: "macos",
} as const;

export type ArtifactArchitecture =
  (typeof ArtifactArchitecture)[keyof typeof ArtifactArchitecture];

export const ArtifactArchitecture = {
  x86_64: "x86_64",
  arm64: "arm64",
} as const;

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ArtifactMinRequirements = { [key: string]: unknown };

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ArtifactCompiler = { [key: string]: unknown };

/**
 * A downloadable, per-OS/architecture build of a release. sha256 is used by the Launcher to verify binary integrity in case the distribution source is ever compromised.
 */
export interface Artifact {
  os: ArtifactOs;
  architecture: ArtifactArchitecture;
  download_url: string;
  /**
   * Lowercase hex-encoded SHA-256 of the artifact file.
   * @pattern ^[a-f0-9]{64}$
   */
  sha256: string;
  size_bytes: number;
  /** Free-form (validated at the API layer, not by this schema alone). */
  min_requirements: ArtifactMinRequirements;
  /** Free-form (validated at the API layer, not by this schema alone). */
  compiler: ArtifactCompiler;
}

export interface Profile {
  id: string;
  full_name: string;
  /** Public/signed URL resolved from the stored avatar_path, not the raw path itself. */
  avatar_url?: string | null;
}

export interface Team {
  id: string;
  name: string;
  member_count: number;
  created_at?: string;
}

export type TeamMemberRole =
  (typeof TeamMemberRole)[keyof typeof TeamMemberRole];

export const TeamMemberRole = {
  owner: "owner",
  admin: "admin",
  member: "member",
} as const;

export interface TeamMember {
  user_id: string;
  role: TeamMemberRole;
  joined_at: string;
}

export type MembershipRequestType =
  (typeof MembershipRequestType)[keyof typeof MembershipRequestType];

export const MembershipRequestType = {
  join_request: "join_request",
  invite: "invite",
  ownership_transfer: "ownership_transfer",
} as const;

export type MembershipRequestStatus =
  (typeof MembershipRequestStatus)[keyof typeof MembershipRequestStatus];

export const MembershipRequestStatus = {
  pending: "pending",
  accepted: "accepted",
  rejected: "rejected",
  cancelled: "cancelled",
} as const;

/**
 * A pending, accepted, rejected, or cancelled team membership action. type=join_request: user_id requested to join, an owner/admin decides. type=invite: an owner/admin invited user_id, who decides. type=ownership_transfer: the owner offered ownership to user_id, who decides.
 */
export interface MembershipRequest {
  id: string;
  team_id: string;
  user_id: string;
  type: MembershipRequestType;
  initiated_by: string;
  status: MembershipRequestStatus;
  requested_at: string;
}

export interface Project {
  id: string;
  owner_id: string;
  team_id?: string | null;
  name: string;
  description?: string | null;
  archive_size_bytes: number;
  /** @pattern ^[a-f0-9]{64}$ */
  archive_sha256: string;
}

export type NewsStatus = (typeof NewsStatus)[keyof typeof NewsStatus];

export const NewsStatus = {
  draft: "draft",
  published: "published",
} as const;

export interface News {
  id: string;
  title: string;
  slug: string;
  body?: string;
  author_id?: string;
  status: NewsStatus;
  published_at?: string | null;
}

export type PollOptionsItem = {
  id: string;
  label: string;
  display_order?: number;
};

export interface Poll {
  id: string;
  question: string;
  closes_at?: string | null;
  options: PollOptionsItem[];
}

export interface PollResult {
  option_id: string;
  label: string;
  vote_count: number;
}

export interface Discussion {
  id: string;
  title: string;
  body?: string;
  author_id: string;
  category?: string | null;
  is_locked: boolean;
  created_at?: string;
}

export interface DiscussionReply {
  id: string;
  discussion_id: string;
  author_id: string;
  /** null/omitted when deleted_at is set -- render as "[deleted]". */
  body?: string | null;
  deleted_at?: string | null;
  created_at?: string;
}

export type DocSourceKind = (typeof DocSourceKind)[keyof typeof DocSourceKind];

export const DocSourceKind = {
  engine_mdbook: "engine_mdbook",
  tutorial: "tutorial",
  other: "other",
} as const;

export interface DocSource {
  id: string;
  kind: DocSourceKind;
  title: string;
  slug: string;
  base_url: string;
  current_version_ref?: string | null;
}

/**
 * A search hit. Read the full page at the matching source's base_url + path -- this API only ever returns a snippet, never the full page content.
 */
export interface DocSearchResult {
  page_id: string;
  source_id: string;
  source_slug: string;
  path: string;
  title: string;
  snippet?: string | null;
  rank: number;
}

export interface Error {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
}

export type ListReleasesParams = {
  status?: ListReleasesStatus;
  /**
   * @minimum 1
   * @maximum 100
   */
  limit?: number;
  /**
   * @minimum 0
   */
  offset?: number;
};

export type ListReleasesStatus =
  (typeof ListReleasesStatus)[keyof typeof ListReleasesStatus];

export const ListReleasesStatus = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type ListReleases200ItemStatus =
  (typeof ListReleases200ItemStatus)[keyof typeof ListReleases200ItemStatus];

export const ListReleases200ItemStatus = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type ListReleases200ItemArtifactsItemOs =
  (typeof ListReleases200ItemArtifactsItemOs)[keyof typeof ListReleases200ItemArtifactsItemOs];

export const ListReleases200ItemArtifactsItemOs = {
  windows: "windows",
  linux: "linux",
  macos: "macos",
} as const;

export type ListReleases200ItemArtifactsItemArchitecture =
  (typeof ListReleases200ItemArtifactsItemArchitecture)[keyof typeof ListReleases200ItemArtifactsItemArchitecture];

export const ListReleases200ItemArtifactsItemArchitecture = {
  x86_64: "x86_64",
  arm64: "arm64",
} as const;

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ListReleases200ItemArtifactsItemMinRequirements = {
  [key: string]: unknown;
};

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type ListReleases200ItemArtifactsItemCompiler = {
  [key: string]: unknown;
};

/**
 * A downloadable, per-OS/architecture build of a release. sha256 is used by the Launcher to verify binary integrity in case the distribution source is ever compromised.
 */
export type ListReleases200ItemArtifactsItem = {
  os: ListReleases200ItemArtifactsItemOs;
  architecture: ListReleases200ItemArtifactsItemArchitecture;
  download_url: string;
  /**
   * Lowercase hex-encoded SHA-256 of the artifact file.
   * @pattern ^[a-f0-9]{64}$
   */
  sha256: string;
  size_bytes: number;
  /** Free-form (validated at the API layer, not by this schema alone). */
  min_requirements: ListReleases200ItemArtifactsItemMinRequirements;
  /** Free-form (validated at the API layer, not by this schema alone). */
  compiler: ListReleases200ItemArtifactsItemCompiler;
};

/**
 * Coreverse Engine release metadata. Contains no binaries -- see artifacts[] for per-OS/architecture downloads.
 */
export type ListReleases200Item = {
  /** @pattern ^\d+\.\d+\.\d+$ */
  version: string;
  status: ListReleases200ItemStatus;
  release_date: string;
  release_notes_summary?: string | null;
  artifacts: ListReleases200ItemArtifactsItem[];
};

export type ListReleases400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetLatestReleaseParams = {
  status?: GetLatestReleaseStatus;
};

export type GetLatestReleaseStatus =
  (typeof GetLatestReleaseStatus)[keyof typeof GetLatestReleaseStatus];

export const GetLatestReleaseStatus = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type GetLatestRelease200Status =
  (typeof GetLatestRelease200Status)[keyof typeof GetLatestRelease200Status];

export const GetLatestRelease200Status = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type GetLatestRelease200ArtifactsItemOs =
  (typeof GetLatestRelease200ArtifactsItemOs)[keyof typeof GetLatestRelease200ArtifactsItemOs];

export const GetLatestRelease200ArtifactsItemOs = {
  windows: "windows",
  linux: "linux",
  macos: "macos",
} as const;

export type GetLatestRelease200ArtifactsItemArchitecture =
  (typeof GetLatestRelease200ArtifactsItemArchitecture)[keyof typeof GetLatestRelease200ArtifactsItemArchitecture];

export const GetLatestRelease200ArtifactsItemArchitecture = {
  x86_64: "x86_64",
  arm64: "arm64",
} as const;

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type GetLatestRelease200ArtifactsItemMinRequirements = {
  [key: string]: unknown;
};

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type GetLatestRelease200ArtifactsItemCompiler = {
  [key: string]: unknown;
};

/**
 * A downloadable, per-OS/architecture build of a release. sha256 is used by the Launcher to verify binary integrity in case the distribution source is ever compromised.
 */
export type GetLatestRelease200ArtifactsItem = {
  os: GetLatestRelease200ArtifactsItemOs;
  architecture: GetLatestRelease200ArtifactsItemArchitecture;
  download_url: string;
  /**
   * Lowercase hex-encoded SHA-256 of the artifact file.
   * @pattern ^[a-f0-9]{64}$
   */
  sha256: string;
  size_bytes: number;
  /** Free-form (validated at the API layer, not by this schema alone). */
  min_requirements: GetLatestRelease200ArtifactsItemMinRequirements;
  /** Free-form (validated at the API layer, not by this schema alone). */
  compiler: GetLatestRelease200ArtifactsItemCompiler;
};

/**
 * Coreverse Engine release metadata. Contains no binaries -- see artifacts[] for per-OS/architecture downloads.
 */
export type GetLatestRelease200 = {
  /** @pattern ^\d+\.\d+\.\d+$ */
  version: string;
  status: GetLatestRelease200Status;
  release_date: string;
  release_notes_summary?: string | null;
  artifacts: GetLatestRelease200ArtifactsItem[];
};

export type GetLatestRelease404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetReleaseByVersion200Status =
  (typeof GetReleaseByVersion200Status)[keyof typeof GetReleaseByVersion200Status];

export const GetReleaseByVersion200Status = {
  stable: "stable",
  beta: "beta",
  rc: "rc",
  deprecated: "deprecated",
} as const;

export type GetReleaseByVersion200ArtifactsItemOs =
  (typeof GetReleaseByVersion200ArtifactsItemOs)[keyof typeof GetReleaseByVersion200ArtifactsItemOs];

export const GetReleaseByVersion200ArtifactsItemOs = {
  windows: "windows",
  linux: "linux",
  macos: "macos",
} as const;

export type GetReleaseByVersion200ArtifactsItemArchitecture =
  (typeof GetReleaseByVersion200ArtifactsItemArchitecture)[keyof typeof GetReleaseByVersion200ArtifactsItemArchitecture];

export const GetReleaseByVersion200ArtifactsItemArchitecture = {
  x86_64: "x86_64",
  arm64: "arm64",
} as const;

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type GetReleaseByVersion200ArtifactsItemMinRequirements = {
  [key: string]: unknown;
};

/**
 * Free-form (validated at the API layer, not by this schema alone).
 */
export type GetReleaseByVersion200ArtifactsItemCompiler = {
  [key: string]: unknown;
};

/**
 * A downloadable, per-OS/architecture build of a release. sha256 is used by the Launcher to verify binary integrity in case the distribution source is ever compromised.
 */
export type GetReleaseByVersion200ArtifactsItem = {
  os: GetReleaseByVersion200ArtifactsItemOs;
  architecture: GetReleaseByVersion200ArtifactsItemArchitecture;
  download_url: string;
  /**
   * Lowercase hex-encoded SHA-256 of the artifact file.
   * @pattern ^[a-f0-9]{64}$
   */
  sha256: string;
  size_bytes: number;
  /** Free-form (validated at the API layer, not by this schema alone). */
  min_requirements: GetReleaseByVersion200ArtifactsItemMinRequirements;
  /** Free-form (validated at the API layer, not by this schema alone). */
  compiler: GetReleaseByVersion200ArtifactsItemCompiler;
};

/**
 * Coreverse Engine release metadata. Contains no binaries -- see artifacts[] for per-OS/architecture downloads.
 */
export type GetReleaseByVersion200 = {
  /** @pattern ^\d+\.\d+\.\d+$ */
  version: string;
  status: GetReleaseByVersion200Status;
  release_date: string;
  release_notes_summary?: string | null;
  artifacts: GetReleaseByVersion200ArtifactsItem[];
};

export type GetReleaseByVersion400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetReleaseByVersion404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateTeamBody = {
  name: string;
};

export type CreateTeam201 = {
  id: string;
  name: string;
  member_count: number;
  created_at?: string;
};

export type CreateTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RenameTeamBody = {
  name: string;
};

export type RenameTeam200 = {
  id: string;
  name: string;
  member_count: number;
  created_at?: string;
};

export type RenameTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RenameTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RenameTeam403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteTeam403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListTeamMembers200ItemRole =
  (typeof ListTeamMembers200ItemRole)[keyof typeof ListTeamMembers200ItemRole];

export const ListTeamMembers200ItemRole = {
  owner: "owner",
  admin: "admin",
  member: "member",
} as const;

export type ListTeamMembers200Item = {
  user_id: string;
  role: ListTeamMembers200ItemRole;
  joined_at: string;
};

export type ListTeamMembers400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListTeamMembers401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type TeamMemberActionBodyAction =
  (typeof TeamMemberActionBodyAction)[keyof typeof TeamMemberActionBodyAction];

export const TeamMemberActionBodyAction = {
  promote: "promote",
  demote: "demote",
  remove: "remove",
} as const;

export type TeamMemberActionBody = {
  action: TeamMemberActionBodyAction;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type TeamMemberAction200 = {
  ok: boolean;
};

export type TeamMemberAction400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type TeamMemberAction401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type TeamMemberAction403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RequestToJoinTeam201Type =
  (typeof RequestToJoinTeam201Type)[keyof typeof RequestToJoinTeam201Type];

export const RequestToJoinTeam201Type = {
  join_request: "join_request",
  invite: "invite",
  ownership_transfer: "ownership_transfer",
} as const;

export type RequestToJoinTeam201Status =
  (typeof RequestToJoinTeam201Status)[keyof typeof RequestToJoinTeam201Status];

export const RequestToJoinTeam201Status = {
  pending: "pending",
  accepted: "accepted",
  rejected: "rejected",
  cancelled: "cancelled",
} as const;

/**
 * A pending, accepted, rejected, or cancelled team membership action. type=join_request: user_id requested to join, an owner/admin decides. type=invite: an owner/admin invited user_id, who decides. type=ownership_transfer: the owner offered ownership to user_id, who decides.
 */
export type RequestToJoinTeam201 = {
  id: string;
  team_id: string;
  user_id: string;
  type: RequestToJoinTeam201Type;
  initiated_by: string;
  status: RequestToJoinTeam201Status;
  requested_at: string;
};

export type RequestToJoinTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RequestToJoinTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RequestToJoinTeam409 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type InviteToTeamBody = {
  user_id: string;
};

export type InviteToTeam201Type =
  (typeof InviteToTeam201Type)[keyof typeof InviteToTeam201Type];

export const InviteToTeam201Type = {
  join_request: "join_request",
  invite: "invite",
  ownership_transfer: "ownership_transfer",
} as const;

export type InviteToTeam201Status =
  (typeof InviteToTeam201Status)[keyof typeof InviteToTeam201Status];

export const InviteToTeam201Status = {
  pending: "pending",
  accepted: "accepted",
  rejected: "rejected",
  cancelled: "cancelled",
} as const;

/**
 * A pending, accepted, rejected, or cancelled team membership action. type=join_request: user_id requested to join, an owner/admin decides. type=invite: an owner/admin invited user_id, who decides. type=ownership_transfer: the owner offered ownership to user_id, who decides.
 */
export type InviteToTeam201 = {
  id: string;
  team_id: string;
  user_id: string;
  type: InviteToTeam201Type;
  initiated_by: string;
  status: InviteToTeam201Status;
  requested_at: string;
};

export type InviteToTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type InviteToTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type InviteToTeam403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type OfferOwnershipTransferBody = {
  user_id: string;
};

export type OfferOwnershipTransfer201Type =
  (typeof OfferOwnershipTransfer201Type)[keyof typeof OfferOwnershipTransfer201Type];

export const OfferOwnershipTransfer201Type = {
  join_request: "join_request",
  invite: "invite",
  ownership_transfer: "ownership_transfer",
} as const;

export type OfferOwnershipTransfer201Status =
  (typeof OfferOwnershipTransfer201Status)[keyof typeof OfferOwnershipTransfer201Status];

export const OfferOwnershipTransfer201Status = {
  pending: "pending",
  accepted: "accepted",
  rejected: "rejected",
  cancelled: "cancelled",
} as const;

/**
 * A pending, accepted, rejected, or cancelled team membership action. type=join_request: user_id requested to join, an owner/admin decides. type=invite: an owner/admin invited user_id, who decides. type=ownership_transfer: the owner offered ownership to user_id, who decides.
 */
export type OfferOwnershipTransfer201 = {
  id: string;
  team_id: string;
  user_id: string;
  type: OfferOwnershipTransfer201Type;
  initiated_by: string;
  status: OfferOwnershipTransfer201Status;
  requested_at: string;
};

export type OfferOwnershipTransfer400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type OfferOwnershipTransfer401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type OfferOwnershipTransfer403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type LeaveTeam400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type LeaveTeam401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type LeaveTeam403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type AcceptMembershipRequest200 = {
  ok: boolean;
};

export type AcceptMembershipRequest400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type AcceptMembershipRequest401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type AcceptMembershipRequest403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type AcceptMembershipRequest404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type AcceptMembershipRequest409 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type RejectMembershipRequest200 = {
  ok: boolean;
};

export type RejectMembershipRequest400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RejectMembershipRequest401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RejectMembershipRequest403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RejectMembershipRequest404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RejectMembershipRequest409 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type CancelMembershipRequest200 = {
  ok: boolean;
};

export type CancelMembershipRequest400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CancelMembershipRequest401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CancelMembershipRequest403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetMyProfile200 = {
  id: string;
  full_name: string;
  /** Public/signed URL resolved from the stored avatar_path, not the raw path itself. */
  avatar_url?: string | null;
};

export type GetMyProfile401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetMyProfile404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

/**
 * At least one of full_name or avatar_path must be provided.
 */
export type UpdateMyProfileBody = {
  /**
   * @minLength 1
   * @maxLength 100
   */
  full_name?: string;
  /**
   * Storage path in the avatars bucket. Set this directly only if you already know a valid path (e.g. clearing the avatar with null); to upload a new image, use POST /profiles/me/avatar instead, which uploads the file and sets this for you.
   * @minLength 1
   */
  avatar_path?: string | null;
};

export type UpdateMyProfile200 = {
  id: string;
  full_name: string;
  /** Public/signed URL resolved from the stored avatar_path, not the raw path itself. */
  avatar_url?: string | null;
};

export type UpdateMyProfile400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateMyProfile401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateMyProfile404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UploadMyAvatarBody = {
  /** PNG or WebP image, up to 5 MB. */
  file: Blob;
};

export type UploadMyAvatar200 = {
  id: string;
  full_name: string;
  /** Public/signed URL resolved from the stored avatar_path, not the raw path itself. */
  avatar_url?: string | null;
};

export type UploadMyAvatar400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UploadMyAvatar401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UploadMyAvatar404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UploadMyAvatar413 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RequestPasswordResetBody = {
  email: string;
  /** Where the recovery link should land. Must be an exact `https://<allowed-website-origin>/{locale}/reset-password` URL (no query string or hash) -- the origin is checked against the deploy environment's allowed origins and the path against the known locale list. A redirectTo that doesn't match either is rejected with 400 invalid_redirect rather than silently falling back to Supabase Auth's Site URL. */
  redirectTo: string;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type RequestPasswordReset200 = {
  ok: boolean;
};

export type RequestPasswordReset400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type RequestPasswordReset429 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListProjectsParams = {
  team_id?: string;
};

export type ListProjects200Item = {
  id: string;
  owner_id: string;
  team_id?: string | null;
  name: string;
  description?: string | null;
  archive_size_bytes: number;
  /** @pattern ^[a-f0-9]{64}$ */
  archive_sha256: string;
};

export type ListProjects400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateProjectBody = {
  name: string;
  description?: string | null;
  team_id?: string | null;
  archive_path: string;
  archive_size_bytes: number;
  /** @pattern ^[a-f0-9]{64}$ */
  archive_sha256: string;
};

export type CreateProject201 = {
  id: string;
  owner_id: string;
  team_id?: string | null;
  name: string;
  description?: string | null;
  archive_size_bytes: number;
  /** @pattern ^[a-f0-9]{64}$ */
  archive_sha256: string;
};

export type CreateProject400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateProject401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateProjectBody = {
  name?: string;
  description?: string | null;
};

export type UpdateProject200 = {
  id: string;
  owner_id: string;
  team_id?: string | null;
  name: string;
  description?: string | null;
  archive_size_bytes: number;
  /** @pattern ^[a-f0-9]{64}$ */
  archive_sha256: string;
};

export type UpdateProject400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateProject404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteProject400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteProject404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetProjectDownloadUrl200 = {
  url: string;
  /** Seconds until the URL expires. */
  expires_in: number;
};

export type GetProjectDownloadUrl400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetProjectDownloadUrl404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListNewsParams = {
  status?: ListNewsStatus;
};

export type ListNewsStatus =
  (typeof ListNewsStatus)[keyof typeof ListNewsStatus];

export const ListNewsStatus = {
  draft: "draft",
  published: "published",
} as const;

export type ListNews200ItemStatus =
  (typeof ListNews200ItemStatus)[keyof typeof ListNews200ItemStatus];

export const ListNews200ItemStatus = {
  draft: "draft",
  published: "published",
} as const;

export type ListNews200Item = {
  id: string;
  title: string;
  slug: string;
  body?: string;
  author_id?: string;
  status: ListNews200ItemStatus;
  published_at?: string | null;
};

export type ListNews400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateNewsBody = {
  title: string;
  slug: string;
  body: string;
};

export type CreateNews201Status =
  (typeof CreateNews201Status)[keyof typeof CreateNews201Status];

export const CreateNews201Status = {
  draft: "draft",
  published: "published",
} as const;

export type CreateNews201 = {
  id: string;
  title: string;
  slug: string;
  body?: string;
  author_id?: string;
  status: CreateNews201Status;
  published_at?: string | null;
};

export type CreateNews400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateNews401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateNews403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateNews409 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateNewsBodyStatus =
  (typeof UpdateNewsBodyStatus)[keyof typeof UpdateNewsBodyStatus];

export const UpdateNewsBodyStatus = {
  draft: "draft",
  published: "published",
} as const;

export type UpdateNewsBody = {
  title?: string;
  body?: string;
  status?: UpdateNewsBodyStatus;
};

export type UpdateNews200Status =
  (typeof UpdateNews200Status)[keyof typeof UpdateNews200Status];

export const UpdateNews200Status = {
  draft: "draft",
  published: "published",
} as const;

export type UpdateNews200 = {
  id: string;
  title: string;
  slug: string;
  body?: string;
  author_id?: string;
  status: UpdateNews200Status;
  published_at?: string | null;
};

export type UpdateNews400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateNews403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateNews404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteNews400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteNews404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListPolls200ItemOptionsItem = {
  id: string;
  label: string;
  display_order?: number;
};

export type ListPolls200Item = {
  id: string;
  question: string;
  closes_at?: string | null;
  options: ListPolls200ItemOptionsItem[];
};

export type CreatePollBody = {
  question: string;
  closes_at?: string | null;
  /** @minItems 2 */
  options: string[];
};

export type CreatePoll201OptionsItem = {
  id: string;
  label: string;
  display_order?: number;
};

export type CreatePoll201 = {
  id: string;
  question: string;
  closes_at?: string | null;
  options: CreatePoll201OptionsItem[];
};

export type CreatePoll400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreatePoll403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CastVoteBody = {
  option_id: string;
};

/**
 * Trivial acknowledgement body for actions that don't return a resource.
 */
export type CastVote201 = {
  ok: boolean;
};

export type CastVote400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CastVote401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CastVote409 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type GetPollResults200Item = {
  option_id: string;
  label: string;
  vote_count: number;
};

export type GetPollResults400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListDiscussionsParams = {
  category?: string;
};

export type ListDiscussions200Item = {
  id: string;
  title: string;
  body?: string;
  author_id: string;
  category?: string | null;
  is_locked: boolean;
  created_at?: string;
};

export type CreateDiscussionBody = {
  title: string;
  body: string;
  category?: string | null;
};

export type CreateDiscussion201 = {
  id: string;
  title: string;
  body?: string;
  author_id: string;
  category?: string | null;
  is_locked: boolean;
  created_at?: string;
};

export type CreateDiscussion400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type CreateDiscussion401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussionBody = {
  title?: string;
  body?: string;
  is_locked?: boolean;
};

export type UpdateDiscussion200 = {
  id: string;
  title: string;
  body?: string;
  author_id: string;
  category?: string | null;
  is_locked: boolean;
  created_at?: string;
};

export type UpdateDiscussion400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussion403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussion404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteDiscussion400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type DeleteDiscussion404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListDiscussionReplies200Item = {
  id: string;
  discussion_id: string;
  author_id: string;
  /** null/omitted when deleted_at is set -- render as "[deleted]". */
  body?: string | null;
  deleted_at?: string | null;
  created_at?: string;
};

export type ListDiscussionReplies400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ReplyToDiscussionBody = {
  body: string;
};

export type ReplyToDiscussion201 = {
  id: string;
  discussion_id: string;
  author_id: string;
  /** null/omitted when deleted_at is set -- render as "[deleted]". */
  body?: string | null;
  deleted_at?: string | null;
  created_at?: string;
};

export type ReplyToDiscussion400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ReplyToDiscussion401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ReplyToDiscussion403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussionReplyBody = {
  body?: string;
  /** Set true to soft-delete. */
  deleted?: boolean;
};

export type UpdateDiscussionReply200 = {
  id: string;
  discussion_id: string;
  author_id: string;
  /** null/omitted when deleted_at is set -- render as "[deleted]". */
  body?: string | null;
  deleted_at?: string | null;
  created_at?: string;
};

export type UpdateDiscussionReply400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussionReply403 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type UpdateDiscussionReply404 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ListDocSources200ItemKind =
  (typeof ListDocSources200ItemKind)[keyof typeof ListDocSources200ItemKind];

export const ListDocSources200ItemKind = {
  engine_mdbook: "engine_mdbook",
  tutorial: "tutorial",
  other: "other",
} as const;

export type ListDocSources200Item = {
  id: string;
  kind: ListDocSources200ItemKind;
  title: string;
  slug: string;
  base_url: string;
  current_version_ref?: string | null;
};

export type SearchDocsParams = {
  q: string;
  kind?: SearchDocsKind;
  /**
   * @minimum 1
   * @maximum 50
   */
  limit?: number;
};

export type SearchDocsKind =
  (typeof SearchDocsKind)[keyof typeof SearchDocsKind];

export const SearchDocsKind = {
  engine_mdbook: "engine_mdbook",
  tutorial: "tutorial",
  other: "other",
} as const;

/**
 * A search hit. Read the full page at the matching source's base_url + path -- this API only ever returns a snippet, never the full page content.
 */
export type SearchDocs200Item = {
  page_id: string;
  source_id: string;
  source_slug: string;
  path: string;
  title: string;
  snippet?: string | null;
  rank: number;
};

export type SearchDocs400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ReindexDocsBodySourceKind =
  (typeof ReindexDocsBodySourceKind)[keyof typeof ReindexDocsBodySourceKind];

export const ReindexDocsBodySourceKind = {
  engine_mdbook: "engine_mdbook",
  tutorial: "tutorial",
  other: "other",
} as const;

export type ReindexDocsBodySource = {
  kind: ReindexDocsBodySourceKind;
  title: string;
  slug: string;
  base_url: string;
  current_version_ref?: string | null;
};

export type ReindexDocsBodyPagesItem = {
  path: string;
  title: string;
  content_text?: string;
};

export type ReindexDocsBody = {
  source: ReindexDocsBodySource;
  /** @minItems 1 */
  pages: ReindexDocsBodyPagesItem[];
};

export type ReindexDocs200 = {
  source_id?: string;
  upserted?: number;
  pruned?: number;
};

export type ReindexDocs400 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};

export type ReindexDocs401 = {
  /** Short machine-readable error code, e.g. "invalid_version". */
  error: string;
  /** Human-readable explanation. */
  message: string;
};
